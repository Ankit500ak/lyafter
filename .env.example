# Lyftr AI Webhook API - Environment Variables Example
# Copy this file to .env and fill in actual values

# REQUIRED: HMAC secret for webhook signature verification
# Must be set for /health/ready to return 200
WEBHOOK_SECRET=your-secret-key-here

# Default: sqlite:////data/app.db (in Docker container)
# For local dev: sqlite:///./app.db
DATABASE_URL=sqlite:////data/app.db

# Logging level
# Options: DEBUG, INFO, WARNING, ERROR
# Default: INFO
LOG_LEVEL=INFO
